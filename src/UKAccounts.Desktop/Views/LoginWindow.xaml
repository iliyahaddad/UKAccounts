<Window x:Class="UKAccounts.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UK Accounts - Login" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="UK Accounts" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Text="Username" Grid.Row="1" Margin="0,0,0,5"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="2" Height="30" Margin="0,0,0,15"/>

        <TextBlock Text="Password" Grid.Row="3" Margin="0,0,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Height="30" Margin="0,0,0,15"/>

        <Button Content="Login" Grid.Row="5" Height="35" Click="LoginButton_Click"/>

        <TextBlock x:Name="ErrorTextBlock" Grid.Row="6" Foreground="Red" HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>
