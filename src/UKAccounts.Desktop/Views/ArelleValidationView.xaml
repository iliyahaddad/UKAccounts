<Window x:Class="UKAccounts.Desktop.Views.ArelleValidationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iXBRL Validation" Height="500" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="iXBRL Validation Results" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock x:Name="StatusTextBlock" Text="Ready" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="DurationTextBlock" Text="" VerticalAlignment="Center"/>
        </StackPanel>

        <TabControl Grid.Row="2" Margin="0,0,0,10">
            <TabItem Header="Errors">
                <DataGrid x:Name="ErrorsDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="150"/>
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="400"/>
                        <DataGridTextColumn Header="Line" Binding="{Binding LineNumber}" Width="50"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Warnings">
                <DataGrid x:Name="WarningsDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="150"/>
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="400"/>
                        <DataGridTextColumn Header="Line" Binding="{Binding LineNumber}" Width="50"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Info">
                <DataGrid x:Name="InfosDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="150"/>
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="400"/>
                        <DataGridTextColumn Header="Line" Binding="{Binding LineNumber}" Width="50"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="80" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
